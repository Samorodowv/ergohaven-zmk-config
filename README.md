# Imperial44 VOSKHOD Symbol Map

`num_sym` layer is active while holding `MO(2)` from `base` or `ru`.

## Base symbols (EN layer)

```text
Row 1: [ Q ][ W ][ E ][ R ][ T ]          [ Y ][ U ][ I ][ O ][ P ][ [ ]
Row 2: [ A ][ S ][ D ][ F ][ G ]          [ H ][ J ][ K ][ L ][ ; ][ ] ]
Row 3: [ Z ][ X ][ C ][ V ][ B ] [ -][ -] [ N ][ M ][ , ][ . ][ ' ][ / (Alt hold) ]
Thumb: [ LCtrl] [ MO(2)][ Space]          [ Enter ][ MO(3) ][ Backspace ]
```

## Symbols + numpad (`num_sym`)

```text
Row 1: [ `(Gui hold) ][ % ][ # ][ $ ][ ^ ][ _ ]    [ - ][ 7 ][ 8 ][ 9 ][ + ][ = ]
Row 2: [ Tab         ][ @ ][ ! ][ & ][ ( ][ ) ]    [ * ][ 4 ][ 5 ][ 6 ][ 0 ][ LCtrl ]
Row 3: [ LShift      ][ Ins ][ Caps ][ -- ][ \ ]   [ Del ][ -- ][ -- ][ / ][ 1 ][ 2 ][ 3 ][ . ][ LAlt ]
Thumb: [ LCtrl][ Trans ][ Space ]                  [ Enter ][ MO(4) ][ Backspace ]
```

## RU note

On `ru` layer, the rightmost bottom key is `alt_lang_ru`: tap is `RU_DOT`, hold is `Alt`, and `Shift+that key` triggers language toggle.
